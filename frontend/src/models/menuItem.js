export class menuItem{
    constructor(itemName, itemPrice, itemDescription){
        this.itemName = itemName;
        this.itemPrice = itemPrice;
        this.itemDescription = itemDescription;
    }
}